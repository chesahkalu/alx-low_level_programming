#include <stdio.h>
#include <ctype.h>
/**
 * main - is entry point
 *
 * Return: 1, if succesful
 */
int main(void)
{
	int x;

	for (x = 0 ; x < 10 ; x++)
	printf("%d", x);
	printf("\n");
	return (0);
}
